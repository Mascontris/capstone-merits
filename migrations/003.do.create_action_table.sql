CREATE TABLE action (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    description TEXT NOT NULL,
    kid_id INTEGER REFERENCES kid(id) ON DELETE CASCADE NOT NULL,
    polarity BOOLEAN, 
    created_at TIMESTAMP DEFAULT now() NOT NULL
)