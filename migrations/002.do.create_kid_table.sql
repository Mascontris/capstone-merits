CREATE TABLE kid (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    dob DATE,
    household_id INTEGER REFERENCES household(id) ON DELETE CASCADE NOT NULL,
    current_stars SMALLINT, 
    created_at TIMESTAMP DEFAULT now() NOT NULL
)